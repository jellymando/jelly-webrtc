export enum CONNECTION_EVENT {
  OFFER = "offer",
  ANSWER = "answer",
  CANDIDATE = "candidate",
  CLOSE = "close"
}

export enum CONNECTION_STATE {
  CONNECTED = "connected",
  DISCONNECTED = "disconnected",
  FAILED = "failed"
}
